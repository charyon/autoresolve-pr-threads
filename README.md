# autoresolve-pr-threads — retired

This GitHub Action was retired on September 13, 2026 and is no longer supported or recommended. The repository is archived for historical reference.

Remove every workflow step that uses `charyon/autoresolve-pr-threads` or its former `odysseustech/autoresolve-pr-threads` name. Remove any required check dedicated to this action when removing its workflow. No replacement is prescribed.

Archiving does not revoke existing tags or commit references: older pinned copies may still execute in downstream repositories. Consumers must remove their references explicitly. Existing releases and history are retained; no tags were moved.

The current Charyon repository tooling is developed in [repository-kit](https://github.com/charyon/repository-kit), with company policy in the private engineering-policy repository. This does not imply that its automation is activated.
